{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "lldb build task",
            "type": "shell",
            "command": "${workspaceFolder}/gradlew assembleDebug",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}